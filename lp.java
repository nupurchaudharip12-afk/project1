public class GreetingClass {
    // A simple method to return a greeting
    public static String sayHello(String name) {
        String message = 'Hello, ' + name + '!';
        System.debug(message); // Prints to the debug log
        return message;
    }
}
